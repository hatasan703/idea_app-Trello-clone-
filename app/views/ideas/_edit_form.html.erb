<div class="modal-dialog" role="document">
  <div class="modal-content">
    <%= form_with(model: @idea, local: true) do |f| %>
      <div class="modal-header">
        <h5 class="modal-title">New memo</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <div class="form-group field">
          <p>
            <%= f.label :title, "タイトル", class: 'form-control-label' %><br>
            <%= f.text_field :title , class: 'form-control', required: true %><br>
          </p>
          <p>
            <%= f.label :content, "本文", class: 'form-control-label' %><br>
            <%= f.text_area :content, class: 'form-control', required: true %><br>
          </p>
        </div>
        <div class="form_fields">
          <%= f.fields_for :memos, @memos do |memo| %>
            <%= f.label :content, "メモ", class: 'form-control-label' %><br>
            <%= memo.text_field :content, class: 'form-control' %>
            <%= memo.hidden_field :user_id, value: current_user.id %>
          <% end %>
        </div>
      </div>
      <div class="modal-footer actions">
        <%= f.submit class: "btn btn-primary"%>
      </div>
    <% end %>
  </div>
</div>
